name: ARK Self Build (Dogfooding)

on:
  workflow_dispatch:
  push:
    branches: [main, develop, develop2]
    paths:
      - "pycompiler_ark.py"
      - "cli/**"
      - "Core/**"
      - "EngineLoader/**"
      - "ENGINES/**"
      - "requirements.txt"
      - ".github/workflows/ark-self-build.yml"

jobs:
  self-compile:
    name: Self-compile with ARK CLI (pyinstaller)
    runs-on: ubuntu-latest
    env:
      QT_QPA_PLATFORM: offscreen
      PYTHONUNBUFFERED: "1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install runtime dependencies
        run: python -m pip install -r requirements.txt

      - name: Install build engine dependency (pyinstaller)
        run: python -m pip install pyinstaller

      - name: ARK self-build pipeline
        shell: bash
        run: |
          set -euo pipefail
          ARK_BIN="python3 pycompiler_ark.py"
          WORKSPACE_DIR="$GITHUB_WORKSPACE"
          ENTRYPOINT_FILE="$WORKSPACE_DIR/pycompiler_ark.py"
          REPORT_DIR="$WORKSPACE_DIR/.ark_ci_artifacts"

          mkdir -p "$REPORT_DIR"

          $ARK_BIN init "$WORKSPACE_DIR" --with-venv --json > "$REPORT_DIR/init.json"
          $ARK_BIN cfg-auto "$WORKSPACE_DIR" --entrypoint "pycompiler_ark.py" --json > "$REPORT_DIR/cfg_auto.json"
          $ARK_BIN check "$WORKSPACE_DIR" --json --strict > "$REPORT_DIR/check.json"
          $ARK_BIN engine info pyinstaller --workspace "$WORKSPACE_DIR" --json > "$REPORT_DIR/engine_info.json"
          $ARK_BIN engine compile pyinstaller "$ENTRYPOINT_FILE" --workspace "$WORKSPACE_DIR" --json > "$REPORT_DIR/build_result.json"

      - name: Collect artifacts
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ci_artifacts
          if [ -d dist ]; then
            cp -r dist ci_artifacts/dist
          fi
          if [ -d build ]; then
            cp -r build ci_artifacts/build
          fi
          if [ -d .ark ]; then
            cp -r .ark ci_artifacts/workspace_dot_ark
          fi
          if [ -d .ark_ci_artifacts ]; then
            cp -r .ark_ci_artifacts ci_artifacts/reports
          fi

      - name: Upload self-build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ark-self-build-${{ github.run_id }}
          path: ci_artifacts/**
          if-no-files-found: warn
          retention-days: 14
