/* PyCompiler ARK — Slate Theme (ui/themes/dark.qss)
   Palette sombre contrastée avec accent cyan
*/

/* Base */
QWidget {
  background: #101317; /* presque noir, confortable */
  color: #E6E8EB;
  font-family: "Segoe UI", "Noto Sans", "DejaVu Sans", Arial, sans-serif;
  font-size: 10pt;
}

QToolTip {
  background-color: #1A1F25;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 6px;
  padding: 6px 8px;
}

/* Titres / labels */
QLabel[objectName="label_workspace_section"],
QLabel[objectName="label_files_section"],
QLabel[objectName="label_options_section"],
QLabel[objectName="label_logs_section"] {
  font-weight: 600;
  font-size: 12pt;
  color: #F1F3F5;
  padding-bottom: 4px;
}

QLabel[objectName="label_main_actions"],
QLabel[objectName="label_config_actions"],
QLabel[objectName="label_tools"],
QLabel[objectName="label_settings"],
QLabel[objectName="label_progress"] {
  font-weight: 600;
  font-size: 11pt;
  color: #F1F3F5;
  padding-bottom: 4px;
}

/* Boutons */
QPushButton {
  background: #1A1F25;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 10px;
  padding: 8px 14px;
  font-size: 10pt;
}
QPushButton:hover {
  background: #222833;
  border-color: #414A55;
}
QPushButton:pressed {
  background: #1F252D;
}
QPushButton:disabled {
  color: #878B93;
  background: #1A1D21;
  border-color: #1F2329;
}

/* Boutons principaux */
QPushButton#btn_build_all {
  background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #5CCCD1, stop:1 #4CC2C8);
  color: #ffffff;
  border-color: #4CC2C8;
  font-weight: 600;
}
QPushButton#btn_build_all:hover { 
  background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #66D4D9, stop:1 #54C7CD);
  border-color: #5CCCD1;
}
QPushButton#btn_build_all:pressed { 
  background: #3AAAB0; 
}
QPushButton#btn_build_all:disabled {
  background: #1A1D21;
  color: #55595F;
  border-color: #1F2329;
}
QPushButton#btn_cancel_all {
  background: #3A2023;
  color: #ffb3ba;
  border: 1px solid #5A2A30;
}
QPushButton#btn_cancel_all:hover { 
  background: #44262A;
  border-color: #6A3A40;
}
QPushButton#btn_cancel_all:disabled {
  background: #1A1D21;
  color: #55595F;
  border-color: #1F2329;
}

QLineEdit:disabled, QTextEdit:disabled, QPlainTextEdit:disabled, QListWidget:disabled, QTreeWidget:disabled, QTableWidget:disabled, QTableView:disabled, QComboBox:disabled, QSpinBox:disabled, QDoubleSpinBox:disabled, QDateEdit:disabled, QDateTimeEdit:disabled {
  background: #1A1D21;
  color: #55595F;
  border-color: #1F2329;
}

/* Cases à cocher */
QCheckBox { color: #E6E8EB; }
QCheckBox::indicator {
  width: 16px; height: 16px;
}
QCheckBox::indicator:unchecked {
  border: 1px solid #414A55; border-radius: 3px; background: #13171C;
}
QCheckBox::indicator:checked {
  image: none; border: 1px solid #4CC2C8; background: #4CC2C8;
}

/* Champs de saisie */
QLineEdit, QTextEdit, QPlainTextEdit {
  background: #13171C;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 6px;
  padding: 6px 8px;
}
QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus { border-color: #4CC2C8; }

/* Listes */
QListWidget, QTreeWidget, QTableWidget, QTableView {
  background: #13171C;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 8px;
  selection-background-color: #24455A;
  selection-color: #E6E8EB;
  padding: 4px;
}
QListWidget::item, QTreeWidget::item {
  padding: 6px 8px;
  border-radius: 4px;
}
QListWidget::item:hover, QTreeWidget::item:hover {
  background: #1A1F25;
}
QListWidget::item:selected, QTreeWidget::item:selected {
  background: #24455A;
  color: #E6E8EB;
}
QListWidget::item:alternate {
  background: #151A20;
}

/* Onglets */
QTabWidget::pane {
  border: 1px solid #2B323C; border-radius: 8px; background: #13171C;
}
QTabBar { background: #13171C; }
QTabBar::tab {
  background: #1A1F25; color: #E6E8EB; padding: 6px 12px; border: 1px solid #2B323C; border-bottom: none; border-top-left-radius: 8px; border-top-right-radius: 8px;
}
QTabBar::tab:selected { background: #13171C; }
QTabBar::tab:hover { background: #222833; }

/* Barre de progression */
QProgressBar {
  border: 1px solid #2B323C; border-radius: 8px; text-align: center; background: #13171C; color: #E6E8EB; height: 14px;
}
QProgressBar::chunk { background-color: #4CC2C8; border-radius: 8px; }

/* ComboBox */
QComboBox, QSpinBox, QDoubleSpinBox {
  background: #13171C; color: #E6E8EB; border: 1px solid #2B323C; border-radius: 6px; padding: 4px 8px;
}
QComboBox:hover, QSpinBox:hover, QDoubleSpinBox:hover { background: #1A1F25; }
QComboBox QAbstractItemView { background: #13171C; color: #E6E8EB; selection-background-color: #24455A; selection-color: #E6E8EB; }

/* Scrollbars (Qt6) */
QScrollBar:vertical {
  background: transparent; width: 12px; margin: 0px; border: none;
}
QScrollBar::handle:vertical {
  background: #414A55; min-height: 24px; border-radius: 6px;
}
QScrollBar::handle:vertical:hover { background: #56606B; }
QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical { height: 0; }

QScrollBar:horizontal {
  background: transparent; height: 12px; margin: 0px; border: none;
}
QScrollBar::handle:horizontal {
  background: #414A55; min-width: 24px; border-radius: 6px;
}
QScrollBar::handle:horizontal:hover { background: #56606B; }
QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal { width: 0; }

/* Dialogs & Message Boxes */
QDialog, QMessageBox {
  background: #13171C;
  color: #E6E8EB;
  border-radius: 12px;
  border: 1px solid #2B323C;
}
QDialog QLabel, QMessageBox QLabel { color: #E6E8EB; }
QDialogButtonBox QPushButton, QMessageBox QPushButton {
  background: #1A1F25;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 10px;
  padding: 6px 12px;
  min-width: 90px;
}
QDialogButtonBox QPushButton:hover, QMessageBox QPushButton:hover { background: #222833; }
QDialogButtonBox QPushButton:pressed, QMessageBox QPushButton:pressed { background: #1F252D; }
QDialogButtonBox QPushButton:default, QMessageBox QPushButton:default {
  background: #4CC2C8; color: #ffffff; border-color: #4CC2C8;
}
QDialogButtonBox QPushButton:default:hover, QMessageBox QPushButton:default:hover { background: #3AAAB0; }

/* Text edit log (monospace) */
QTextEdit#log {
  font-family: "Cascadia Code", "Fira Code", "DejaVu Sans Mono", monospace;
  font-size: 10pt;
  background: #0D1115;
}

/* Modern selects and inputs */
QComboBox {
  background: #13171C;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 10px;
  padding: 6px 36px 6px 10px;
}
QComboBox:focus { border-color: #4CC2C8; }
QComboBox::drop-down {
  subcontrol-origin: padding;
  subcontrol-position: top right;
  width: 30px;
  border-left: 1px solid #2B323C;
  background: #1A1F25;
  border-top-right-radius: 10px;
  border-bottom-right-radius: 10px;
}
QComboBox QAbstractItemView {
  background: #13171C;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 10px;
  padding: 4px;
  selection-background-color: #24455A;
  selection-color: #E6E8EB;
  outline: 0;
}
QComboBox QAbstractItemView::item { min-height: 26px; }

QAbstractSpinBox {
  background: #13171C;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 10px;
  padding: 6px 8px;
}
QAbstractSpinBox:focus { border-color: #4CC2C8; }
QSpinBox::up-button, QDoubleSpinBox::up-button {
  subcontrol-origin: border;
  subcontrol-position: top right;
  width: 22px;
  border-left: 1px solid #2B323C;
  background: #1A1F25;
  border-top-right-radius: 10px;
}
QSpinBox::down-button, QDoubleSpinBox::down-button {
  subcontrol-origin: border;
  subcontrol-position: bottom right;
  width: 22px;
  border-left: 1px solid #2B323C;
  background: #1A1F25;
  border-bottom-right-radius: 10px;
}

QRadioButton::indicator {
  width: 16px; height: 16px;
  border: 1px solid #414A55; border-radius: 8px; background: #13171C;
}
QRadioButton::indicator:checked {
  background: #4CC2C8; border-color: #4CC2C8;
}

/* Frames et groupes */
QFrame[frameShape="4"] {
  background: #151A20;
  border: 1px solid #2B323C;
  border-radius: 12px;
}

QGroupBox { 
  border: 1px solid #2B323C; 
  border-radius: 10px; 
  margin-top: 12px;
  padding-top: 8px;
}
QGroupBox::title { 
  subcontrol-origin: margin; 
  left: 12px; 
  padding: 0 6px; 
  background: transparent; 
  color: #E6E8EB;
  font-weight: 600;
}

/* Sidebar styling */
QScrollArea#sidebarScrollArea {
  background: #0D1115;
  border-right: 1px solid #2B323C;
}
QScrollArea#sidebarScrollArea > QWidget {
  background: #0D1115;
}

/* Main content area */
QScrollArea#dashboardScrollArea {
  background: #101317;
}


/* === Coverage: hidden widgets === */
QToolButton {
  background: palette(button);
  color: palette(text);
  border: 1px solid palette(mid);
  border-radius: 8px;
  padding: 6px 10px;
}
QToolButton:hover { background: palette(light); }
QToolButton:pressed, QToolButton:checked { background: palette(mid); }
QToolButton::menu-button { border: none; width: 16px; }

QCommandLinkButton {
  background: palette(button);
  color: palette(text);
  border: 1px solid palette(mid);
  border-radius: 10px;
  padding: 8px 12px;
}
QCommandLinkButton:hover { background: palette(light); }
QCommandLinkButton:pressed { background: palette(mid); }
QCommandLinkButton:disabled { color: palette(mid); background: palette(window); border-color: palette(mid); }

QToolBox::tab {
  background: palette(button);
  color: palette(text);
  border: 1px solid palette(mid);
  border-top-left-radius: 8px;
  border-top-right-radius: 8px;
  padding: 6px 10px;
}
QToolBox::tab:selected { background: palette(base); }
QToolBox QWidget { background: palette(window); }

QDockWidget {
  background: palette(window);
  border: 1px solid palette(mid);
}
QDockWidget::title {
  text-align: left;
  padding: 6px 10px;
  background: palette(base);
  color: palette(text);
  border-bottom: 1px solid palette(mid);
}
QDockWidget::close-button, QDockWidget::float-button {
  border: none;
  background: transparent;
  padding: 2px;
}
QDockWidget::close-button:hover, QDockWidget::float-button:hover {
  background: palette(light);
}

QCalendarWidget {
  background: palette(base);
  border: 1px solid palette(mid);
  border-radius: 8px;
}
QCalendarWidget QWidget#qt_calendar_navigationbar {
  background: palette(button);
  border-bottom: 1px solid palette(mid);
}
QCalendarWidget QToolButton {
  background: palette(button);
  color: palette(text);
  border: 1px solid palette(mid);
  border-radius: 6px;
  padding: 4px 8px;
}
QCalendarWidget QToolButton:hover { background: palette(light); }
QCalendarWidget QSpinBox { background: palette(base); border: 1px solid palette(mid); }
QCalendarWidget QAbstractItemView {
  background: palette(base);
  selection-background-color: palette(highlight);
  selection-color: palette(highlighted-text);
}
QCalendarWidget QAbstractItemView:disabled { color: palette(mid); }

QDateEdit, QTimeEdit, QDateTimeEdit {
  background: palette(base);
  color: palette(text);
  border: 1px solid palette(mid);
  border-radius: 6px;
  padding: 4px 8px;
}
QDateEdit:focus, QTimeEdit:focus, QDateTimeEdit:focus { border-color: palette(highlight); }

QSlider::groove:horizontal { height: 6px; background: palette(midlight); border-radius: 3px; }
QSlider::handle:horizontal { width: 16px; height: 16px; margin: -5px 0; border-radius: 8px; background: palette(highlight); border: 1px solid palette(mid); }
QSlider::sub-page:horizontal { background: palette(highlight); border-radius: 3px; }
QSlider::add-page:horizontal { background: palette(midlight); border-radius: 3px; }
QSlider::groove:vertical { width: 6px; background: palette(midlight); border-radius: 3px; }
QSlider::handle:vertical { width: 16px; height: 16px; margin: 0 -5px; border-radius: 8px; background: palette(highlight); border: 1px solid palette(mid); }
QSlider::sub-page:vertical { background: palette(highlight); border-radius: 3px; }
QSlider::add-page:vertical { background: palette(midlight); border-radius: 3px; }
QSlider:disabled { color: palette(mid); }

QLCDNumber {
  color: palette(highlight);
  background: palette(base);
  border: 1px solid palette(mid);
  border-radius: 6px;
  padding: 4px;
}

QToolBar {
  background: palette(window);
  border-bottom: 1px solid palette(mid);
  spacing: 6px;
}
QToolBar::separator { background: palette(mid); width: 1px; height: 1px; margin: 4px; }

/* === Theme polish === */
QHeaderView::section {
  background: #1A1F25;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  padding: 6px 10px;
}
QHeaderView::section:hover { background: #222833; }

QMenuBar {
  background: #151A20;
  color: #E6E8EB;
  border-bottom: 1px solid #2B323C;
}
QMenuBar::item { padding: 6px 10px; background: transparent; border-radius: 8px; }
QMenuBar::item:selected { background: #222833; }

QMenu {
  background: #13171C;
  color: #E6E8EB;
  border: 1px solid #2B323C;
  border-radius: 10px;
}
QMenu::item { padding: 6px 12px; }
QMenu::item:selected { background: #24455A; }
QMenu::separator { height: 1px; background: #2B323C; margin: 6px 8px; }

QStatusBar {
  background: #151A20;
  color: #878B93;
  border-top: 1px solid #2B323C;
}
QStatusBar::item { border: none; }

QListWidget::item:hover, QTreeWidget::item:hover, QTableView::item:hover {
  background: #24455A;
}
